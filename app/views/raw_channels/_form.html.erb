<%= form_for(@raw_channel) do |f| %>
  <% if @raw_channel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@raw_channel.errors.count, "error") %> prohibited this raw_channel from being saved:</h2>

      <ul>
      <% @raw_channel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :xmltv_id %><br />
    <%= f.text_field :xmltv_id %>
  </div>
  <div class="field">
    <%= f.label :channel_name %><br />
    <%= f.text_field :channel_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

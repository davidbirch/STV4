<%= semantic_form_for @sport, {:html => { :class => "form-horizontal" }} do |f| %>
  <%= f.inputs do %>
    <%= f.input :name %>
  <% end %>
  <%= f.actions do %>
      <%= f.action :submit, :button_html => { :class => "btn-mini btn-primary"} %> 
  <% end %>
<% end %>
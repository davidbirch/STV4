<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title><%= title %></title>
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->
    <%= csrf_meta_tags %>
    <%= render "layouts/header_meta_tags" %>
    <%= render "layouts/header_apple_touch_icon" %>    
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= stylesheet_link_tag "custom", :media => "all" %>
  </head>
  <body>
    <div class="container">
      <header class="row centre">
        <div class="span12">  
          <%= render "layouts/header" %>
        </div>
      </header>
      <div class="row main">
        <div class="span3 left_content">
          <%= render "layouts/left_menu_logged_in" %>
          <%= render "layouts/left_menu_search" %>
          <%= render "layouts/left_menu_sport_list" %>
          <%= render "layouts/left_menu_advertisment1" %>
          <%= render "layouts/left_menu_region_list" %>
          <%= render "layouts/left_menu_page_list" %>
          <%= render "layouts/left_menu_news_feed" %>
        </div>
        <div class="span9 right_content">
          <%= render "layouts/main_breadcrumbs" %>
          <%= render "layouts/main_notice" %>
          <%= yield %>
        </div>
      </div>
      <footer class = "upper_footer row">
        <div class="span12">
          <%= render "layouts/footer" %>
        </div>
      </footer>
      <footer class="lower_footer row">
        <div class="span12">
          <% if logged_in? %>
          <%= debug params %>
          <% end %>
        </div>
      </div>
    </div>
    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
    <% if !logged_in? && Rails.env.production? %>
    <!-- Google Analytics -->
    <script>
      var _gaq=[['_setAccount','UA-8415542-1'],['_trackPageview']];
      (function(d,t){var g=d.createElement(t),s=d.getElementsByTagName(t)[0];
      g.src=('https:'==location.protocol?'//ssl':'//www')+'.google-analytics.com/ga.js';
      s.parentNode.insertBefore(g,s)}(document,'script'));
    </script> 
    <% end %>
  </body>
</html>
    
    
    
    
